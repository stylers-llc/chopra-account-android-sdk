apply plugin: 'com.android.library'

android {
    compileSdkVersion 27
    buildToolsVersion "27.0.0"
    defaultConfig {
        minSdkVersion 15
        targetSdkVersion 27
        versionCode 6
        versionName "2.0.2"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    api 'com.scottyab:aescrypt:0.0.1'

    // Facebook SDK for Facebook login
    api 'com.facebook.android:facebook-android-sdk:4.+'

    // Google Play Services for Google login
    api 'com.google.android.gms:play-services-auth:11.+'
}